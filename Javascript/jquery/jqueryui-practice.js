$(document).ready(function () {
    // ---slider in formindex.html
    // $('#slide').slider();
    // $("#slide").slider({orientation:'vertical'})
})


$(document).ready(function () {
    //    ---autocomplete in forminex.html
    // $('#cities').autocomplete({source:['Dallas','Datroi','Hyderbad']},{autoFocus:true});
    // $('#cities').autocomplete({source:['Dallas','Datroi','Hyderbad']},{delay:300});
    // $('#cities').autocomplete({source:['Dallas','Datroi','Hyderbad']},{minLength:3});
})


$(document).ready(function () {
    // --------select menu in formindex.html
    // $('#files').selectmenu();
    // $('#files').selectmenu({width:150});
    // $('#files').selectmenu({width:150},{icons:{button:'ui-icon-arrowthick-1-a'}});
})


$(document).ready(function () {
    // --Resizable in formindex.html
    // $('#resize').resizable();
    // $('#resize').resizable({maxWidth:300,maxHeight:300});
    // $('#resize').resizable({autoHide:true})
    // $('#resize').resizable({distance:300})
    // $('#resize').resizable({ghost:true})
    // $('#resize').resizable({aspectRatio:true})
    // $('#resize').resizable({animate:true})
    // $('#resize').resizable({animateDuration:'fast'})
})

$(document).ready(function () {
    // -------sortable in formindex.html
    // $('.sort').sortable();
    // $('.sort').sortable({opacity:0.5})
    // $('.sort').sortable({distance:150});
    // $('.sort').sortable({delay:350});
    //  $('.sort').sortable({disabled:true});
    // $('.sort').sortable({cursor:'move'});
    // $('.sort').sortable({containment:'parent'});
    // $('.sort').sortable({axis:'y'});
    // $('.sort').sortable({grid:[20,10]});

})

$(document).ready(function () {
    // ---drag in formindex.html
    //$('#drag').draggable();
    // $('.drag').draggable();
    //  $('.drag').draggable({grid:[50,30]});
    //   $('.drag').draggable({containment:'parent'});
    //  $('.drag').draggable({cursor:'crosshair'});
    //  $('.drag').draggable({delay:'350'});
    //  $('.drag').draggable({revert:true});
    //  $('.drag').draggable({axis:'x'});
})

$(document).ready(function () {

    // -----------tabs widget in formindex.html
    // $('#maincontainer').tabs();
    // $('#maincontainer').tabs({active:0})
    // $('#maincontainer').tabs({heightStyle:'fill'}) //content/fill
    // $('#maincontainer').tabs({event:'mouseover'});
    // $('#maincontainer').tabs({disabled:[1,2]});

})

$(document).ready(function () {
    // ----  menu method in formindex.html
    // $('.verticalmenu').menu();
    // $('.verticalmenu').menu({icons:{submenu:'ui-icon-circle-triangle-e'}});
    // $('.verticalmenu').menu({menus:'div'})

    // ---message box
    // $('#box').dialog();
    // $('#box').dialog({title:'test title'});
    // $('#box').dialog({autoOpen:false});
    // $('#box').dialog({draggable:false,resizable:false});
    // $('#box').dialog({height:300,width:400});
    // $('#box').dialog({show:150,hide:1000});
    // $('#box').dialog({show:'bounce',hide:'explode'});
    // $('#box').dialog({closeOnEscape:false});
    // $('#box').dialog({modal: true});

})

$(document).ready(function () {
    // ----- Accordion widgets vertically
    // $('#pannel').accordion();
    //  $('#pannel').accordion({collapsible:true})
    // $('#pannel').accordion({event:'mouseover'})
    // $('#pannel').accordion({animate:2})
    // $('#pannel').accordion({active:1,heightStyle:'content'})

    // --Accordion Icons
    // $('#pannel').accordion({icons: {header:'ui-icon-plus',activeHeader:'ui-icon-minus'}});
    // $('#pannel').accordion({icons: {header:'ui-icon-zoomin',activeHeader:'ui-icon-zoomout'}});
    // $('#pannel').accordion({icons: {header:'ui-icon-mail-closed',activeHeader:'ui-icon-mail-open'}});
    //  $('#pannel').accordion({icons: {header:'ui-icon-folder-collapsed',activeHeader:'ui-icon-folder-open'}});

    // --accordion style
    // edit "jquery-ui.structure.css" then give our own css styles

})

$(document).ready(function () {
    //--- tooltip widgets and options

    // $('img').tooltip();
    // $('img').tooltip({content:'All are clouds'});
    // $('img').tooltip({ track: true })
    // $('img').tooltip({show: {effect:'pulsate',duration:800}})
    // $('img').tooltip({show: {effect:'bounce',duration:800}})
    // $('img').tooltip({show: {effect:'fadeIn',duration:800}})
    // $('img').tooltip({show: {effect:'explode',duration:800}})
    // $('img').tooltip({show: {effect:'blind',duration:800}})
    // $('img').tooltip({show: {effect:'highlight',duration:800}})
    // $('img').tooltip({ show: { effect: 'fadeIn', duration: 800 }},{ hide: { effect: 'blind', duration: 800 }});

    // ---tooltip styling 
    // edit jquery-ui.structure.css then add ur own css styles

})

$(document).ready(function () {

    // --datepicker widgets method and options
    $('input').each(function () {
        //  $(this).datepicker();
    });

    //  $('div #date').datepicker(); 
    // $('#date').datepicker({numberOfMonths:2});
    // $('#date').datepicker({changeMonth:true,showWeek:true,weekHeader: 'week', changeYear:true});
    // $('#date').datepicker({showOtherMonths:true});
    //$('#date').datepicker({minDate: new Date(2019,04,01)})
    //$('#date').datepicker({maxDate: new Date(2020,11,28)})
    //$('#date').datepicker({showButtonPanel: true,currentText:'Current date',closeText:'close Calender'})
    //$('#date').datepicker({yearSuffix:" BC"});
})